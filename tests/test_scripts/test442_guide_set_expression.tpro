{{# Test set with expressions from guide #}}
{{set price := 100}}
{{set quantity := 5}}
{{set total := @(price * quantity)}}
{{set netPrice := @(total / 1.22)}}
Total: {{:total}}
Net: {{:netPrice|round,2}}
